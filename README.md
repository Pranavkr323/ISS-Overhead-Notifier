# ISS-Overhead-Notifier
🚀 ISS Overhead Notifier: A Python automation project that tracks the International Space Station in real time and sends an email alert when the ISS is near my location and it's dark enough for viewing. Uses Open Notify API, Sunrise-Sunset API, Requests, Datetime, and SMTP for automation.
